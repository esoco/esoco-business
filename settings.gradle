rootProject.name = "esoco-business" 

def baseProjects = [':esoco-common',':objectrelations',':esoco-lib', ':esoco-storage']

baseProjects.each { projectName ->
	include projectName
	project(projectName).projectDir = new File("../${projectName.substring(1)}")
}
